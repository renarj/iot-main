apiVersion: v1
kind: Service
metadata:
  name: mqtt-svc
  labels:
    app: mqtt
spec:
  type: NodePort
  ports:
    - port: 1883
      targetPort: 1883
      protocol: TCP
      name: mqtt
      nodePort: 31883
  selector:
    app: mqtt
